export default class Song {
    constructor (
        public title?: string,
        public url?: string,
        public duration?: string,
        public author?: string,
        public thumbnail?: string
    ) {}
}